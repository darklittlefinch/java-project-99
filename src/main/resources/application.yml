spring:
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
  output:
    ansi:
      enabled: always
  profiles:
    default: dev

rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem
